# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace-with-a-long-random-secret
# Generate with: python -c "from argon2 import PasswordHasher; print(PasswordHasher().hash('your-admin-password'))"
ADMIN_PASSWORD_HASH=replace-with-an-argon2id-hash
DATABASE_URL=postgresql+psycopg://goalhoodz:change-me@localhost:5432/goalhoodz
FRONTEND_ORIGIN=http://localhost:5173
# Set to true only behind HTTPS in production.
COOKIE_SECURE=false
